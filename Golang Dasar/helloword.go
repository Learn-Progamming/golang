package main

import "fmt"

func main () {
	// Print ouput dengan teks "Hellow World"
	fmt.Println("Hello World")

	/* 
	Untuk menjalankan program bisa menggunakan 'go run name_file.go', atau build terlebih dahulu dengan syntax 'go build name_file.go'
	setelah build beres, akan menghasilkan generate file. Dan bisa menjalankan menggunakan perintah './name_file'
	*/
}